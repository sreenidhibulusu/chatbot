# Flask Chatbot with OpenAI Demo/Experimentation

This project is a simple Flask web chatbot for experimenting with OpenAI's API.

**How to run:**
1. Set your secrets in a `.env` file (see `.env.example`).
2. Install dependencies:
   ```bash
   pip install -r requirements.txt